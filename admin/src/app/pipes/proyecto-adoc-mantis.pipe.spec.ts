import { ProyectoADocMantisPipe } from './proyecto-adoc-mantis.pipe';

describe('ProyectoADocMantisPipe', () => {
  it('create an instance', () => {
    const pipe = new ProyectoADocMantisPipe();
    expect(pipe).toBeTruthy();
  });
});
