import { FaseAEstadoPipe } from './fase-aestado.pipe';

describe('FaseAEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FaseAEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
