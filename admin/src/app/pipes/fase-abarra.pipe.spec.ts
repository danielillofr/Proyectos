import { FaseABarraPipe } from './fase-abarra.pipe';

describe('FaseABarraPipe', () => {
  it('create an instance', () => {
    const pipe = new FaseABarraPipe();
    expect(pipe).toBeTruthy();
  });
});
