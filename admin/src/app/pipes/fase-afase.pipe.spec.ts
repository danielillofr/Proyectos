import { FaseAFasePipe } from './fase-afase.pipe';

describe('FaseAFasePipe', () => {
  it('create an instance', () => {
    const pipe = new FaseAFasePipe();
    expect(pipe).toBeTruthy();
  });
});
