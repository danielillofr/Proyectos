import { ProyectoATestProbadoPipe } from './proyecto-atest-probado.pipe';

describe('ProyectoATestProbadoPipe', () => {
  it('create an instance', () => {
    const pipe = new ProyectoATestProbadoPipe();
    expect(pipe).toBeTruthy();
  });
});
